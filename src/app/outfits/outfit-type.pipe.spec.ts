import { OutfitTypePipe } from './outfit-type.pipe';

describe('OutfitTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OutfitTypePipe();
    expect(pipe).toBeTruthy();
  });
});
